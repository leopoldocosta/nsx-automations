# Every value is a shell-injection attempt — the parser must reject all of them.
[BAD]
jump_host = dc-bad.example.com; rm -rf /
jump_user = nsxops$(touch /tmp/pwn)
repo_path = /home/nsxops/`whoami`/nsx
ssh_key   = ../../etc/shadow

# A second section with one legit field + one malicious field
[PARTIAL]
jump_host = 10.30.0.10
jump_user = ok-user
repo_path = /tmp/$(id)
